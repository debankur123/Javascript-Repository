console.log(Math.PI);
/*
   1. The trunc method returns all the integer parts and truncates the fractional part
   2. Thge round method returns an integer rounded to its nearest value
 */
console.log(Math.trunc(Math.PI))
console.log(Math.round(Math.PI))
